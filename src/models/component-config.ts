// * 组件配置
export class ComponentConfig {
    // * 生成的文件前缀
    prefix: string = "";

    // * 生成的文件后缀
    suffix: string = "";

    // * template 里的文本内容
    templates: string = "";

    // * css加载器类型
    styleLang: string = "";

    // * 模版类型
    templateLang: string = "";

     // * css作用域
    styleScope: string = "";
}