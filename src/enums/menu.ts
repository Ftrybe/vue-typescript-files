// * 菜单类型
export enum Menu {
    // * 组件 
    component = "component",

    // * 类
    class = "class",

    // * 接口
    interface = "interface",

    // * 枚举
    enum = "enum",

    // * vuex
    vuexModule = "vuex",

    // * 声明文件
    declare = "declare",
    
    // * 指令
    directive = "directive"
}
